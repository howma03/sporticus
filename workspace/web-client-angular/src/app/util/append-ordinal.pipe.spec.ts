import {AppendOrdinalPipe} from './append-ordinal.pipe';

describe('AppendOrdinalPipe', () => {
  it('create an instance', () => {
    const pipe = new AppendOrdinalPipe();
    expect(pipe).toBeTruthy();
  });
});
